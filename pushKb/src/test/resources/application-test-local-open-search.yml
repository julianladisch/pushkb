micronaut:
  http:
    services:
      search:
        url: "http://localhost:9201"
        ssl:
          enabled: true
          insecure-trust-all-certificates: true
elasticsearch:
  http-hosts: "http://localhost:9201"
